name: Publish wgsl4k artifacts to Maven Central

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
  workflow_dispatch:
    inputs:
      version:
        description: Stable release version (for example 1.0.0)
        required: true
        type: string

jobs:
  publish:
    if: (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/master') || (github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v'))
    runs-on: ubuntu-latest
    env:
      MAVEN_CENTRAL_USERNAME: ${{ secrets.MAVEN_CENTRAL_USERNAME }}
      MAVEN_CENTRAL_PASSWORD: ${{ secrets.MAVEN_CENTRAL_PASSWORD }}
      SIGNING_KEY: ${{ secrets.SIGNING_KEY }}
      SIGNING_PASSWORD: ${{ secrets.SIGNING_PASSWORD }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: zulu
          java-version: '25'
          cache: gradle

      - name: Grant execute permission for Gradle wrapper
        run: chmod +x gradlew

      - name: Validate WGSL ABI
        run: >-
          ./gradlew :wgsl:wgsl-core:checkKotlinAbi :wgsl:wgsl-parser:checkKotlinAbi
          :wgsl:wgsl-generator:checkKotlinAbi :wgsl:wgsl-tests:checkKotlinAbi
          :wgsl:wgsl-cli:checkKotlinAbi --no-daemon --stacktrace

      - name: Validate release version
        env:
          INPUT_VERSION: ${{ github.event.inputs.version }}
        run: |
          set -euo pipefail
          if [[ "${{ github.event_name }}" == "push" ]]; then
            version="${GITHUB_REF_NAME#v}"
          else
            version="${INPUT_VERSION:-}"
          fi
          if [[ ! "$version" =~ ^[0-9]+\.[0-9]+\.[0-9]+([.-][0-9A-Za-z.-]+)?$ || "$version" == *SNAPSHOT* ]]; then
            echo "A stable semantic release version is required; got '$version'." >&2
            exit 1
          fi
          echo "VERSION=$version" >> "$GITHUB_ENV"

      - name: Validate publishing credentials
        run: |
          set -euo pipefail
          for name in MAVEN_CENTRAL_USERNAME MAVEN_CENTRAL_PASSWORD SIGNING_KEY SIGNING_PASSWORD; do
            if [[ -z "${!name}" ]]; then
              echo "Required publishing secret $name is unavailable." >&2
              exit 1
            fi
          done

      - name: Publish WGSL artifacts
        run: >-
          ./gradlew :wgsl:wgsl-core:publishToMavenCentral :wgsl:wgsl-parser:publishToMavenCentral
          :wgsl:wgsl-generator:publishToMavenCentral :wgsl:wgsl-tests:publishToMavenCentral
          :wgsl:wgsl-cli:publishToMavenCentral --no-configuration-cache --no-daemon --stacktrace
          -PreleaseVersion="${VERSION}"
        env:
          ORG_GRADLE_PROJECT_mavenCentralUsername: ${{ secrets.MAVEN_CENTRAL_USERNAME }}
          ORG_GRADLE_PROJECT_mavenCentralPassword: ${{ secrets.MAVEN_CENTRAL_PASSWORD }}
          ORG_GRADLE_PROJECT_signingInMemoryKey: ${{ secrets.SIGNING_KEY }}
          ORG_GRADLE_PROJECT_signingInMemoryKeyPassword: ${{ secrets.SIGNING_PASSWORD }}
