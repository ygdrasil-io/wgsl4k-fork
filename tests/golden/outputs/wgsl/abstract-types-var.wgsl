// Generated by WebGPU-KTypes WGSL Backend
var<private> xvipaiai: vec2<i32> = vec2<f32>(42, 43);
var<private> xvupaiai: vec2<u32> = vec2<f32>(44, 45);
var<private> xvfpaiai: vec2<f32> = vec2<f32>(46, 47);
var<private> xvfpafaf: vec2<f32> = vec2<f32>(48.0f, 49.0f);
var<private> xvfpaiaf: vec2<f32> = vec2<f32>(48, 49.0f);
var<private> xvupuai: vec2<u32> = vec2<f32>(42u, 43);
var<private> xvupaiu: vec2<u32> = vec2<f32>(42, 43u);
var<private> xvuuai: vec2<u32> = vec2<u32>(42u, 43);
var<private> xvuaiu: vec2<u32> = vec2<u32>(42, 43u);
var<private> xvip____: vec2<i32> = vec2<f32>();
var<private> xvup____: vec2<u32> = vec2<f32>();
var<private> xvfp____: vec2<f32> = vec2<f32>();
var<private> xmfp____: mat2x2<f32> = mat2x2<f32>(vec2<f32>(), vec2<f32>());
var<private> xmfpaiaiaiai: mat2x2<f32> = mat2x2<f32>(1, 2, 3, 4);
var<private> xmfpafaiaiai: mat2x2<f32> = mat2x2<f32>(1.0f, 2, 3, 4);
var<private> xmfpaiafaiai: mat2x2<f32> = mat2x2<f32>(1, 2.0f, 3, 4);
var<private> xmfpaiaiafai: mat2x2<f32> = mat2x2<f32>(1, 2, 3.0f, 4);
var<private> xmfpaiaiaiaf: mat2x2<f32> = mat2x2<f32>(1, 2, 3, 4.0f);
var<private> xvispai: vec2<i32> = vec2<f32>(1);
var<private> xvfspaf: vec2<f32> = vec2<f32>(1.0f);
var<private> xvis_ai: vec2<i32> = vec2<i32>(1);
var<private> xvus_ai: vec2<u32> = vec2<u32>(1);
var<private> xvfs_ai: vec2<f32> = vec2<f32>(1);
var<private> xvfs_af: vec2<f32> = vec2<f32>(1.0f);
var<private> xafafaf: array<f32, 2> = array<f32, 2>(1.0f, 2.0f);
var<private> xafaiai: array<f32, 2> = array<f32, 2>(1, 2);
var<private> xaipaiai: array<i32, 2> = array<i32, 2>(1, 2);
var<private> xaupaiai: array<u32, 2> = array<u32, 2>(1, 2);
var<private> xafpaiaf: array<f32, 2> = array<f32, 2>(1, 2.0f);
var<private> xafpafai: array<f32, 2> = array<f32, 2>(1.0f, 2);
var<private> xafpafaf: array<f32, 2> = array<f32, 2>(1.0f, 2.0f);
var<private> xavipai: array<vec3<i32>, 1> = array<vec3<i32>, 1>(vec3<f32>(1));
var<private> xavfpai: array<vec3<f32>, 1> = array<vec3<f32>, 1>(vec3<f32>(1));
var<private> xavfpaf: array<vec3<f32>, 1> = array<vec3<f32>, 1>(vec3<f32>(1.0f));
var<private> xvisai: vec2<i32> = vec2<f32>(1);
var<private> xvusai: vec2<u32> = vec2<f32>(1);
var<private> xvfsai: vec2<f32> = vec2<f32>(1);
var<private> xvfsaf: vec2<f32> = vec2<f32>(1.0f);
var<private> ivispai: vec2<f32> = vec2<f32>(1);
var<private> ivfspaf: vec2<f32> = vec2<f32>(1.0f);
var<private> ivis_ai: vec2<i32> = vec2<i32>(1);
var<private> ivus_ai: vec2<u32> = vec2<u32>(1);
var<private> ivfs_ai: vec2<f32> = vec2<f32>(1);
var<private> ivfs_af: vec2<f32> = vec2<f32>(1.0f);
var<private> iafafaf: array<f32, 2> = array<f32, 2>(1.0f, 2.0f);
var<private> iafaiai: array<f32, 2> = array<f32, 2>(1, 2);
var<private> iaipaiai: array<i32, 2> = array<i32, 2>(1, 2);
var<private> iafpafaf: array<f32, 2> = array<f32, 2>(1.0f, 2.0f);
var<private> iafpaiaf: array<i32, 2> = array<i32, 2>(1, 2.0f);
var<private> iafpafai: array<f32, 2> = array<f32, 2>(1.0f, 2);
var<private> iavipai: array<vec3<f32>, 1> = array<vec3<f32>, 1>(vec3<f32>(1));
var<private> iavfpai: array<vec3<f32>, 1> = array<vec3<f32>, 1>(vec3<f32>(1));
var<private> iavfpaf: array<vec3<f32>, 1> = array<vec3<f32>, 1>(vec3<f32>(1.0f));

fn mixed_constant_and_runtime_arguments() {
    var u: u32;
    var i: i32;
    var f: f32;
    var xvupuai: vec2<u32> = vec2<f32>(u, 43);
    var xvupaiu: vec2<u32> = vec2<f32>(42, u);
    var xvfpfai: vec2<f32> = vec2<f32>(f, 47);
    var xvfpfaf: vec2<f32> = vec2<f32>(f, 49.0f);
    var xvuuai: vec2<u32> = vec2<u32>(u, 43);
    var xvuaiu: vec2<u32> = vec2<u32>(42, u);
    var xmfp_faiaiai: mat2x2<f32> = mat2x2<f32>(f, 2, 3, 4);
    var xmfpai_faiai: mat2x2<f32> = mat2x2<f32>(1, f, 3, 4);
    var xmfpaiai_fai: mat2x2<f32> = mat2x2<f32>(1, 2, f, 4);
    var xmfpaiaiai_f: mat2x2<f32> = mat2x2<f32>(1, 2, 3, f);
    var xaf_faf: array<f32, 2> = array<f32, 2>(f, 2.0f);
    var xafaf_f: array<f32, 2> = array<f32, 2>(1.0f, f);
    var xaf_fai: array<f32, 2> = array<f32, 2>(f, 2);
    var xafai_f: array<f32, 2> = array<f32, 2>(1, f);
    var xai_iai: array<i32, 2> = array<i32, 2>(i, 2);
    var xaiai_i: array<i32, 2> = array<i32, 2>(1, i);
    var xafp_faf: array<f32, 2> = array<f32, 2>(f, 2.0f);
    var xafpaf_f: array<f32, 2> = array<f32, 2>(1.0f, f);
    var xafp_fai: array<f32, 2> = array<f32, 2>(f, 2);
    var xafpai_f: array<f32, 2> = array<f32, 2>(1, f);
    var xaip_iai: array<i32, 2> = array<i32, 2>(i, 2);
    var xaipai_i: array<i32, 2> = array<i32, 2>(1, i);
    var xvisi: vec2<i32> = vec2<f32>(i);
    var xvusu: vec2<u32> = vec2<f32>(u);
    var xvfsf: vec2<f32> = vec2<f32>(f);
    xvupuai = vec2<f32>(u, 43);
    xvupaiu = vec2<f32>(42, u);
    xvuuai = vec2<u32>(u, 43);
    xvuaiu = vec2<u32>(42, u);
    xmfp_faiaiai = mat2x2<f32>(f, 2, 3, 4);
    xmfpai_faiai = mat2x2<f32>(1, f, 3, 4);
    xmfpaiai_fai = mat2x2<f32>(1, 2, f, 4);
    xmfpaiaiai_f = mat2x2<f32>(1, 2, 3, f);
    xaf_faf = array<f32, 2>(f, 2.0f);
    xafaf_f = array<f32, 2>(1.0f, f);
    xaf_fai = array<f32, 2>(f, 2);
    xafai_f = array<f32, 2>(1, f);
    xai_iai = array<i32, 2>(i, 2);
    xaiai_i = array<i32, 2>(1, i);
    xafp_faf = array<f32, 2>(f, 2.0f);
    xafpaf_f = array<f32, 2>(1.0f, f);
    xafp_fai = array<f32, 2>(f, 2);
    xafpai_f = array<i32, 2>(1, f);
    xaip_iai = array<i32, 2>(i, 2);
    xaipai_i = array<i32, 2>(1, i);
    xvisi = vec2<f32>(i);
    xvusu = vec2<f32>(u);
    xvfsf = vec2<f32>(f);
}

fn all_constant_arguments() {
    var xvipaiai: vec2<i32> = vec2<f32>(42, 43);
    var xvupaiai: vec2<u32> = vec2<f32>(44, 45);
    var xvfpaiai: vec2<f32> = vec2<f32>(46, 47);
    var xvfpafaf: vec2<f32> = vec2<f32>(48.0f, 49.0f);
    var xvfpaiaf: vec2<f32> = vec2<f32>(48, 49.0f);
    var xvupuai: vec2<u32> = vec2<f32>(42u, 43);
    var xvupaiu: vec2<u32> = vec2<f32>(42, 43u);
    var xvuuai: vec2<u32> = vec2<u32>(42u, 43);
    var xvuaiu: vec2<u32> = vec2<u32>(42, 43u);
    var xvip____: vec2<i32> = vec2<f32>();
    var xvup____: vec2<u32> = vec2<f32>();
    var xvfp____: vec2<f32> = vec2<f32>();
    var xmfp____: mat2x2<f32> = mat2x2<f32>(vec2<f32>(), vec2<f32>());
    var xmfpaiaiaiai: mat2x2<f32> = mat2x2<f32>(1, 2, 3, 4);
    var xmfpafaiaiai: mat2x2<f32> = mat2x2<f32>(1.0f, 2, 3, 4);
    var xmfpaiafaiai: mat2x2<f32> = mat2x2<f32>(1, 2.0f, 3, 4);
    var xmfpaiaiafai: mat2x2<f32> = mat2x2<f32>(1, 2, 3.0f, 4);
    var xmfpaiaiaiaf: mat2x2<f32> = mat2x2<f32>(1, 2, 3, 4.0f);
    var xmfp_faiaiai: mat2x2<f32> = mat2x2<f32>(1.0f, 2, 3, 4);
    var xmfpai_faiai: mat2x2<f32> = mat2x2<f32>(1, 2.0f, 3, 4);
    var xmfpaiai_fai: mat2x2<f32> = mat2x2<f32>(1, 2, 3.0f, 4);
    var xmfpaiaiai_f: mat2x2<f32> = mat2x2<f32>(1, 2, 3, 4.0f);
    var xvispai: vec2<i32> = vec2<f32>(1);
    var xvfspaf: vec2<f32> = vec2<f32>(1.0f);
    var xvis_ai: vec2<i32> = vec2<i32>(1);
    var xvus_ai: vec2<u32> = vec2<u32>(1);
    var xvfs_ai: vec2<f32> = vec2<f32>(1);
    var xvfs_af: vec2<f32> = vec2<f32>(1.0f);
    var xafafaf: array<f32, 2> = array<f32, 2>(1.0f, 2.0f);
    var xaf_faf: array<f32, 2> = array<f32, 2>(1.0f, 2.0f);
    var xafaf_f: array<f32, 2> = array<f32, 2>(1.0f, 2.0f);
    var xafaiai: array<f32, 2> = array<f32, 2>(1, 2);
    var xai_iai: array<i32, 2> = array<i32, 2>(1, 2);
    var xaiai_i: array<i32, 2> = array<i32, 2>(1, 2);
    var xaipaiai: array<i32, 2> = array<i32, 2>(1, 2);
    var xafpaiai: array<f32, 2> = array<f32, 2>(1, 2);
    var xafpaiaf: array<f32, 2> = array<f32, 2>(1, 2.0f);
    var xafpafai: array<f32, 2> = array<f32, 2>(1.0f, 2);
    var xafpafaf: array<f32, 2> = array<f32, 2>(1.0f, 2.0f);
    var xavipai: array<vec3<i32>, 1> = array<vec3<i32>, 1>(vec3<f32>(1));
    var xavfpai: array<vec3<f32>, 1> = array<vec3<f32>, 1>(vec3<f32>(1));
    var xavfpaf: array<vec3<f32>, 1> = array<vec3<f32>, 1>(vec3<f32>(1.0f));
    var xvisai: vec2<i32> = vec2<f32>(1);
    var xvusai: vec2<u32> = vec2<f32>(1);
    var xvfsai: vec2<f32> = vec2<f32>(1);
    var xvfsaf: vec2<f32> = vec2<f32>(1.0f);
    var iaipaiai: array<i32, 2> = array<i32, 2>(1, 2);
    var iafpaiaf: array<i32, 2> = array<i32, 2>(1, 2.0f);
    var iafpafai: array<f32, 2> = array<f32, 2>(1.0f, 2);
    var iafpafaf: array<f32, 2> = array<f32, 2>(1.0f, 2.0f);
    xvipaiai = vec2<f32>(42, 43);
    xvupaiai = vec2<f32>(44, 45);
    xvfpaiai = vec2<f32>(46, 47);
    xvfpafaf = vec2<f32>(48.0f, 49.0f);
    xvfpaiaf = vec2<f32>(48, 49.0f);
    xvupuai = vec2<f32>(42u, 43);
    xvupaiu = vec2<f32>(42, 43u);
    xvuuai = vec2<u32>(42u, 43);
    xvuaiu = vec2<u32>(42, 43u);
    xvip____ = vec2<f32>();
    xvup____ = vec2<f32>();
    xvfp____ = vec2<f32>();
    xmfp____ = mat2x2<f32>(vec2<f32>(), vec2<f32>());
    xmfpaiaiaiai = mat2x2<f32>(1, 2, 3, 4);
    xmfpafaiaiai = mat2x2<f32>(1.0f, 2, 3, 4);
    xmfpaiafaiai = mat2x2<f32>(1, 2.0f, 3, 4);
    xmfpaiaiafai = mat2x2<f32>(1, 2, 3.0f, 4);
    xmfpaiaiaiaf = mat2x2<f32>(1, 2, 3, 4.0f);
    xmfp_faiaiai = mat2x2<f32>(1.0f, 2, 3, 4);
    xmfpai_faiai = mat2x2<f32>(1, 2.0f, 3, 4);
    xmfpaiai_fai = mat2x2<f32>(1, 2, 3.0f, 4);
    xmfpaiaiai_f = mat2x2<f32>(1, 2, 3, 4.0f);
    xvispai = vec2<f32>(1);
    xvfspaf = vec2<f32>(1.0f);
    xvis_ai = vec2<i32>(1);
    xvus_ai = vec2<u32>(1);
    xvfs_ai = vec2<f32>(1);
    xvfs_af = vec2<f32>(1.0f);
    xafafaf = array<f32, 2>(1.0f, 2.0f);
    xaf_faf = array<f32, 2>(1.0f, 2.0f);
    xafaf_f = array<f32, 2>(1.0f, 2.0f);
    xafaiai = array<f32, 2>(1, 2);
    xai_iai = array<i32, 2>(1, 2);
    xaiai_i = array<i32, 2>(1, 2);
    xaipaiai = array<i32, 2>(1, 2);
    xafpaiai = array<i32, 2>(1, 2);
    xafpaiaf = array<i32, 2>(1, 2.0f);
    xafpafai = array<f32, 2>(1.0f, 2);
    xafpafaf = array<f32, 2>(1.0f, 2.0f);
    xavipai = array<vec3<f32>, 1>(vec3<f32>(1));
    xavfpai = array<vec3<f32>, 1>(vec3<f32>(1));
    xavfpaf = array<vec3<f32>, 1>(vec3<f32>(1.0f));
    xvisai = vec2<f32>(1);
    xvusai = vec2<f32>(1);
    xvfsai = vec2<f32>(1);
    xvfsaf = vec2<f32>(1.0f);
    iaipaiai = array<i32, 2>(1, 2);
    iafpaiaf = array<i32, 2>(1, 2.0f);
    iafpafai = array<f32, 2>(1.0f, 2);
    iafpafaf = array<f32, 2>(1.0f, 2.0f);
}

fn globals() {
    _ = xvipaiai;
    _ = xvupaiai;
    _ = xvfpaiai;
    _ = xvfpafaf;
    _ = xvfpaiaf;
    _ = xvupuai;
    _ = xvupaiu;
    _ = xvuuai;
    _ = xvuaiu;
    _ = xvip____;
    _ = xvup____;
    _ = xvfp____;
    _ = xmfp____;
    _ = xmfpaiaiaiai;
    _ = xmfpafaiaiai;
    _ = xmfpaiafaiai;
    _ = xmfpaiaiafai;
    _ = xmfpaiaiaiaf;
    _ = xvispai;
    _ = xvfspaf;
    _ = xvis_ai;
    _ = xvus_ai;
    _ = xvfs_ai;
    _ = xvfs_af;
    _ = xafafaf;
    _ = xafaiai;
    _ = xaipaiai;
    _ = xaupaiai;
    _ = xafpaiaf;
    _ = xafpafai;
    _ = xafpafaf;
    _ = xavipai;
    _ = xavfpai;
    _ = xavfpaf;
    _ = xvisai;
    _ = xvusai;
    _ = xvfsai;
    _ = xvfsaf;
    _ = ivispai;
    _ = ivfspaf;
    _ = ivis_ai;
    _ = ivus_ai;
    _ = ivfs_ai;
    _ = ivfs_af;
    _ = iafafaf;
    _ = iafaiai;
    _ = iaipaiai;
    _ = iafpafaf;
    _ = iafpaiaf;
    _ = iafpafai;
    _ = iavipai;
    _ = iavfpai;
    _ = iavfpaf;
}

@compute
@workgroup_size(1, 1, 1)
fn main() {
    globals();
    all_constant_arguments();
    mixed_constant_and_runtime_arguments();
}
