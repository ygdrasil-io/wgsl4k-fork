// Generated by WebGPU-KTypes WGSL Backend
struct Struct_3 {
    f: f32,
    i: i32,
    u: u32,
}
const xvipaiai: vec2<i32> = vec2<f32>(42, 43);
const xvupaiai: vec2<u32> = vec2<f32>(44, 45);
const xvfpaiai: vec2<f32> = vec2<f32>(46, 47);
const xvfpafaf: vec2<f32> = vec2<f32>(48.0f, 49.0f);
const xvfpaiaf: vec2<f32> = vec2<f32>(48, 49.0f);
const xvupuai: vec2<u32> = vec2<f32>(42u, 43);
const xvupaiu: vec2<u32> = vec2<f32>(42, 43u);
const xvuuai: vec2<u32> = vec2<u32>(42u, 43);
const xvuaiu: vec2<u32> = vec2<u32>(42, 43u);
const xvip____: vec2<i32> = vec2<f32>();
const xvup____: vec2<u32> = vec2<f32>();
const xvfp____: vec2<f32> = vec2<f32>();
const xmfp____: mat2x2<f32> = mat2x2<f32>(vec2<f32>(), vec2<f32>());
const xmfpaiaiaiai: mat2x2<f32> = mat2x2<f32>(1, 2, 3, 4);
const xmfpafaiaiai: mat2x2<f32> = mat2x2<f32>(1.0f, 2, 3, 4);
const xmfpaiafaiai: mat2x2<f32> = mat2x2<f32>(1, 2.0f, 3, 4);
const xmfpaiaiafai: mat2x2<f32> = mat2x2<f32>(1, 2, 3.0f, 4);
const xmfpaiaiaiaf: mat2x2<f32> = mat2x2<f32>(1, 2, 3, 4.0f);
const imfpaiaiaiai: mat2x2<f32> = mat2x2<f32>(1, 2, 3, 4);
const imfpafaiaiai: mat2x2<f32> = mat2x2<f32>(1.0f, 2, 3, 4);
const imfpafafafaf: mat2x2<f32> = mat2x2<f32>(1.0f, 2.0f, 3.0f, 4.0f);
const ivispai: vec2<f32> = vec2<f32>(1);
const ivfspaf: vec2<f32> = vec2<f32>(1.0f);
const ivis_ai: vec2<i32> = vec2<i32>(1);
const ivus_ai: vec2<u32> = vec2<u32>(1);
const ivfs_ai: vec2<f32> = vec2<f32>(1);
const ivfs_af: vec2<f32> = vec2<f32>(1.0f);
const iafafaf: array<f32, 2> = array<f32, 2>(1.0f, 2.0f);
const iafaiai: array<f32, 2> = array<f32, 2>(1, 2);
const iaipaiai: array<i32, 2> = array<i32, 2>(1, 2);
const iafpaiaf: array<i32, 2> = array<i32, 2>(1, 2.0f);
const iafpafai: array<f32, 2> = array<f32, 2>(1.0f, 2);
const iafpafaf: array<f32, 2> = array<f32, 2>(1.0f, 2.0f);
const xaipaiai: array<i32, 2> = array<i32, 2>(1, 2);
const xaupaiai: array<u32, 2> = array<u32, 2>(1, 2);
const xafpaiaf: array<f32, 2> = array<f32, 2>(1, 2.0f);
const xafpafai: array<f32, 2> = array<f32, 2>(1.0f, 2);
const xafpafaf: array<f32, 2> = array<f32, 2>(1.0f, 2.0f);
const xvisai: vec2<i32> = vec2<f32>(1);
const xvusai: vec2<u32> = vec2<f32>(1);
const xvfsai: vec2<f32> = vec2<f32>(1);
const xvfsaf: vec2<f32> = vec2<f32>(1.0f);
const ivfr_f__f: vec3<f32> = vec3<f32>(vec2<f32>(1.0f, 2.0f), 3.0f);
const ivfr_f_af: vec3<f32> = vec3<f32>(vec2<f32>(1.0f, 2.0f), 3.0f);
const ivfraf__f: vec3<f32> = vec3<f32>(vec2<f32>(1.0f, 2.0f), 3.0f);
const ivfraf_af: vec3<f32> = vec3<f32>(vec2<f32>(1.0f, 2.0f), 3.0f);
const ivf__fr_f: vec3<f32> = vec3<f32>(1.0f, vec2<f32>(2.0f, 3.0f));
const ivf__fraf: vec3<f32> = vec3<f32>(1.0f, vec2<f32>(2.0f, 3.0f));
const ivf_afr_f: vec3<f32> = vec3<f32>(1.0f, vec2<f32>(2.0f, 3.0f));
const ivf_afraf: vec3<f32> = vec3<f32>(1.0f, vec2<f32>(2.0f, 3.0f));
const ivfr_f_ai: vec3<f32> = vec3<f32>(vec2<f32>(1.0f, 2.0f), 3);
const ivfrai__f: vec3<f32> = vec3<f32>(vec2<f32>(1, 2), 3.0f);
const ivfrai_ai: vec3<f32> = vec3<f32>(vec2<f32>(1, 2), 3);
const ivf__frai: vec3<f32> = vec3<f32>(1.0f, vec2<f32>(2, 3));
const ivf_air_f: vec3<f32> = vec3<f32>(1, vec2<f32>(2.0f, 3.0f));
const ivf_airai: vec3<f32> = vec3<f32>(1, vec2<f32>(2, 3));
const s_f_i_u: Struct_3 = Struct_3(1.0f, 1, 1u);
const s_f_iai: Struct_3 = Struct_3(1.0f, 1, 1);
const s_fai_u: Struct_3 = Struct_3(1.0f, 1, 1u);
const s_faiai: Struct_3 = Struct_3(1.0f, 1, 1);
const saf_i_u: Struct_3 = Struct_3(1.0f, 1, 1u);
const saf_iai: Struct_3 = Struct_3(1.0f, 1, 1);
const safai_u: Struct_3 = Struct_3(1.0f, 1, 1u);
const safaiai: Struct_3 = Struct_3(1.0f, 1, 1);
